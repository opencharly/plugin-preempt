# 2026.266.1233 — feat: relocate the preempt R10 witness bed from opencharly/charly

## Summary of changes

Relocate the `check-preempt-local` R10 witness (and its `check-preempt-app` `kind: local` template) out of `opencharly/charly`'s `charly.yml` and into `opencharly/plugin-preempt` — the repo that owns the externalized `charly preempt` COMMAND (`candy/plugin-preempt`, `command:preempt`). A check bed belongs beside the code it witnesses: the check-bed runner's dev-tree override (`proc.SelfSuperprojectOverridePair`) points the bed PROJECT's own superproject's `@github` refs at the local tree, and in the umbrella every submodule's superproject is `opencharly/opencharly` (which hosts no candies) — so a plugin candy owned by another repo was never exercised as in-development code while its witness sat in `charly`. Relocated here, the template composes the LOCAL `plugin-preempt` candy with no `@github` pin.

- `charly.yml` (+41/−5) — appends the `check-preempt-app` `kind: local` template (composing the LOCAL `plugin-preempt` candy) and the `check-preempt-local` disposable `host: local` bed, whose sole step runs `charly preempt status` host-side; expands the root-manifest header comment (the `opencharly/charly#459` `discover:` rationale + the relocated-bed note). **This review round** additionally makes the bed's sole step discriminating (finding #6), narrows its description to what the step proves, and corrects the description's stale test citation.
- `.gitignore` (+3/−1) — the generated-artifact rules; replaces `main`'s `*.bak*` with the explicit `*.bak` + `*.bak.*` lines and an explanatory comment.
- `.github/workflows/deploy.yml` — **not in the effective diff**: this branch's original CI pin bump (`v2026.238.1242` → `v2026.256.1316`) was already carried by `main` (via #8); the branch-merge below folds it in, so no deploy.yml change remains.

**Branch brought up to date (resolves the CONFLICTING/DIRTY state).** This branch was behind `main` (which had merged #8's arbiter beds) and conflicted — add/add on `.gitignore`, content on `charly.yml`. The merge keeps BOTH relocated-bed sets: #8's `check-preempt-arbiter-pod` / `check-preempt-live-pod` / `check-preempt-vm-live` + their `check-preempt-fixture` fixture AND this branch's `check-preempt-local`, plus the union of the ignore rules. The description's `check-preempt-arbiter-pod` reference therefore resolves in the PR tree (`charly.yml` line 77), and the branch is no longer behind `main`.

**Finding #6 — the step now discriminates candy/plugin-preempt's output.** The sole step previously asserted only `exit_status: 0` plus a `not_matches` on the bed name, so a hidden core-command forward (or any `preempt status` that exits 0) would pass — the description's "no hidden core-command forward / InvokeProvider peer-dispatch end-to-end" claim was unsubstantiated by the step. This round adds a positive `matches` on candy/plugin-preempt's OWN lease-status output (the `No active preemption leases.` line, or its `CLAIMANT … TOKENS … STATE` lease table under a parallel roster) — a string owned by the plugin's `command.go renderLeaseTable` and **absent from `charly` core** — and narrows the description to what the step actually proves: the externalized command resolves and runs host-side. The INTERNAL dispatch path is now stated as covered by the Go e2e (charly core's `TestArbiterReleaseDispatch_SurvivesAndSurfaces` + this repo's `TestArbiter_ReconcileStrandedRestores`) and the `check-preempt-arbiter-pod` bed, not by this step. The stale citation of the deleted `TestArbiterExternalizedDispatch_AcquirePersistsAndSurfaces` is corrected to the renamed `TestArbiterReleaseDispatch_SurvivesAndSurfaces`.

**Finding #7 — the schema head is reconciled, not contradictory.** `charly.yml`'s `version: 2026.249.2125` is the schema stamp this repo targets — the highest schema version the CI-pinned charly RELEASE `v2026.256.1316` supports (a release version, not a schema version). The upstream spec `#SchemaVersion` HEAD has since advanced to `2026.261.1747` (spec#137); the 249→261 ecosystem wave that will bump this and the sibling plugin configs is tracked in `opencharly/charly#633`. So 2026.249.2125 is the schema head this repo targets and `v2026.256.1316` is the charly release pinned in CI that supports it — no staleness. Verified: both `charly 2026.249.2125` and a newer `charly 2026.258.0220` validate the stamp with 0 warnings.

## Change class

**candy/box-config** — `charly.yml` R10 bed declaration + `.gitignore`; no runtime Go code changed.

**Closes / relates to:** relates to opencharly/charly#625 — the companion PR that removes this same witness (`check-preempt-local` + `check-preempt-app`) from `opencharly/charly` in the same relocation. It must merge; a surviving duplicate is the R3/R5 violation this move eliminates.

**Breaking change + rollback:** the bed/entity moves repos; rollback = revert (companion #625 restores the charly-side witness).

## How R10-tested

- **Gate for this change class:** `charly check run check-preempt-local` + `charly box validate`.
- **Bed:** `check-preempt-local` | **substrate:** local (`host: local`, `disposable: true`) | **ok:** true | **steps:** 6 | **seconds:** 25.
- **Binary:** `charly 2026.249.2125`.
- **Fresh clone at this tree:** `/tmp/opencode/rem-preempt-7` at head `c845ff4`, run with `CHARLY_REPO_OVERRIDE=github.com/opencharly/plugin-preempt=/tmp/opencode/rem-preempt-7` so the bed resolves THIS checkout's `candy/plugin-preempt` rather than a pinned `@github` ref.
- **Changed path ran live:** yes — `check-live` executed the relocated step (`preempt-status-runs`, now carrying the new `matches` assertion) with `exit=0`; `1 step: 1 passed, 0 failed, 0 skipped`.
- **Fresh rebuild:** the bed's `update` stage ran the fresh rebuild and PASSED (`update: 3.58s ok=true`).
- **Concurrency:** N/A — single bed, one disposable deploy.

*1. Gate output (`charly box validate` + `charly check run check-preempt-local`)*:

```
$ cd /tmp/opencode/rem-preempt-7
$ CHARLY_REPO_OVERRIDE="github.com/opencharly/plugin-preempt=/tmp/opencode/rem-preempt-7" /tmp/opencode/charly-now/bin/charly box validate
WARNING: charly is a dev/worktree build (not installed at /usr/bin or /usr/local/bin); NOT loading the installed package's plugins from /usr/lib/charly/plugins (they may be built against a different charly version). Set CHARLY_PLUGIN_DIR to load plugins from a custom location.
charly box validate: OK — checked 4 candies, 1 box, 8 deploys, 7 distros, 7 builders; 0 warnings, 0 errors

$ CHARLY_REPO_OVERRIDE="github.com/opencharly/plugin-preempt=/tmp/opencode/rem-preempt-7" /tmp/opencode/charly-now/bin/charly check run check-preempt-local
WARNING: charly is a dev/worktree build (not installed at /usr/bin or /usr/local/bin); NOT loading the installed package's plugins from /usr/lib/charly/plugins (they may be built against a different charly version). Set CHARLY_PLUGIN_DIR to load plugins from a custom location.
charly: resolving project (first run — may take a moment)...
charly: fetching git metadata for 1 repo(s) (first run — may take a moment)...
charly: git metadata cached.
Resolved @github.com/opencharly/plugin-deploy-local -> v2026.242.0527 (latest tag)
Resolved @github.com/opencharly/plugin-vm -> v2026.262.0752 (latest tag)
charly: fetching git metadata for 2 repo(s) (first run — may take a moment)...
charly: git metadata cached.
plugin github.com/opencharly/plugin-deploy-local/candy/plugin-deploy-local: served from /home/atrawog/.cache/charly/plugins/github.com_opencharly_plugin-deploy-local_candy_plugin-deploy-local-cb999434bd120951 (build stamp 1a5f05ee92f5d34d); source /home/atrawog/.cache/charly/repos/github.com/opencharly/plugin-deploy-local@v2026.242.0527/candy/plugin-deploy-local
WARNING: charly is a dev/worktree build (not installed at /usr/bin or /usr/local/bin); NOT loading the installed package's plugins from /usr/lib/charly/plugins (they may be built against a different charly version). Set CHARLY_PLUGIN_DIR to load plugins from a custom location.
Removed /home/atrawog/.config/containers/systemd/charly-check-preempt-local.container
Reloaded systemd user daemon
charly check run check-preempt-local: [deploy-add] START (charly deploy add; log: .check/check-preempt-local/2026.266.1226/deploy-add.log)
charly check run check-preempt-local: [deploy-add] PASS after 3.575s [allowlisted=1] (log: .check/check-preempt-local/2026.266.1226/deploy-add.log)
charly check run check-preempt-local: [bring-up-members] START
charly check run check-preempt-local: [bring-up-members] PASS after 0s
charly check run check-preempt-local: [check-live] START (charly check live; log: .check/check-preempt-local/2026.266.1226/check-live.log)
charly check run check-preempt-local: [check-live] PASS after 4.532s [allowlisted=1] (log: .check/check-preempt-local/2026.266.1226/check-live.log)
charly check run check-preempt-local: [update] START (charly update; log: .check/check-preempt-local/2026.266.1226/update.log)
charly check run check-preempt-local: [update] PASS after 3.58s [allowlisted=1] (log: .check/check-preempt-local/2026.266.1226/update.log)
charly check run check-preempt-local: [cleanup] START (charly remove; log: .check/check-preempt-local/2026.266.1226/cleanup.log)
charly check run check-preempt-local: [cleanup] PASS after 13.713s [allowlisted=1] (log: .check/check-preempt-local/2026.266.1226/cleanup.log)
charly check run check-preempt-local: [cleanup-members] START
charly check run check-preempt-local: [cleanup-members] PASS after 0s
charly check run check-preempt-local: PASS (steps=6)
```

`.check/check-preempt-local/2026.266.1226/summary.yml`: `ok: true`, `total_seconds: 25`, `errors: 0`, `warnings: 0`; the sole finding is the allowlisted `dev-worktree-binary-notice` (informational dev-build behavior, suppressed 4× — not a defect).

*2. The changed code path executing live — and the new assertion's negative control (finding #6)*:

The relocated step runs candy/plugin-preempt's own command + lease-table renderer against THIS checkout:

```
$ cat .check/check-preempt-local/2026.266.1226/check-live.log
WARNING: charly is a dev/worktree build (not installed at /usr/bin or /usr/local/bin); NOT loading the installed package's plugins from /usr/lib/charly/plugins (they may be built against a different charly version). Set CHARLY_PLUGIN_DIR to load plugins from a custom location.
charly: resolving project (first run — may take a moment)...
Resolved @github.com/opencharly/plugin-deploy-local -> v2026.242.0527 (latest tag)
Resolved @github.com/opencharly/plugin-vm -> v2026.262.0752 (latest tag)
plugin github.com/opencharly/plugin-deploy-local/candy/plugin-deploy-local: served from /home/atrawog/.cache/charly/plugins/github.com_opencharly_plugin-deploy-local_candy_plugin-deploy-local-cb999434bd120951 (build stamp 1a5f05ee92f5d34d); source /home/atrawog/.cache/charly/repos/github.com/opencharly/plugin-deploy-local@v2026.242.0527/candy/plugin-deploy-local
Local deploy: check-preempt-local [host (local)]
  PASS  check command=charly preempt status exits 0 and prints candy/plugin-preempt's own lease status — the "No active preemption leases." line, or its CLAIMANT/…/STATE lease table under a parallel roster — naming no lease owned by THIS bed (bed-owned scoping, because a concurrent sibling bed's transient lease is legitimate; a global no-leases assertion can never hold)  [preempt-status-runs]  exit=0

1 step: 1 passed, 0 failed, 0 skipped
```

The assertion is genuinely discriminating — replacing the expected output with an impossible literal makes the SAME step FAIL, proving the check can now fail on the mechanism it claims (a trivial exit-0 step could not):

```
$ # negative control: same tree, matches: 'NEGATIVE_CONTROL_NEVER_IN_PLUGIN_OUTPUT'
$ CHARLY_REPO_OVERRIDE="github.com/opencharly/plugin-preempt=/tmp/opencode/rem-preempt-7" /tmp/opencode/charly-now/bin/charly check run check-preempt-local
charly check run check-preempt-local: [check-live] START (charly check live; log: .check/check-preempt-local/2026.266.1213/check-live.log)
charly check run check-preempt-local: [check-live] FAIL after 4.686s: exit 2: charly subcommand exited 2 (log: .check/check-preempt-local/2026.266.1213/check-live.log)
charly check run check-preempt-local: FAIL (steps=3)
command "check": charly check run check-preempt-local: checks failed
$ tail -2 .check/check-preempt-local/2026.266.1213/check-live.log
  FAIL  check command=charly preempt status exits 0 and prints candy/plugin-preempt's own lease status …  [preempt-status-runs]  stdout: expected to match /NEGATIVE_CONTROL_NEVER_IN_PLUGIN_OUTPUT/ (got: No active preemption leases.)
1 step: 0 passed, 1 failed, 0 skipped
```

The asserted string is plugin-owned and absent from `charly` core, so it cannot be satisfied by a core-side `preempt` implementation:

```
$ grep -rn "No active preemption leases" /tmp/opencode/charly-pristine/charly/   # charly core (origin/main)
(no matches in core Go source)
$ grep -n "No active preemption leases\|CLAIMANT\\\\tTOKENS" candy/plugin-preempt/command.go
102:		_, _ = fmt.Fprintln(out, "No active preemption leases.")
110:	_, _ = fmt.Fprintln(tw, "CLAIMANT\tTOKENS\tTRANSIENT\tPREEMPTED HOLDERS\tCREATED\tSTATE")
```

*3. Source-side removal proven — opencharly/charly#625 removes this same bed*:

`charly` main still carries the bed; PR #625 (head `7621cc12`) deletes it, and its tree no longer contains it:

```
$ git -C /tmp/opencode/charly-pristine grep -n "check-preempt-local\|check-preempt-app" origin/main -- charly.yml
origin/main:charly.yml:2809:# check-preempt-local — R10 witness for the externalized `charly preempt` COMMAND
origin/main:charly.yml:2826:check-preempt-app:
origin/main:charly.yml:2829:            Minimal kind:local template for the check-preempt-local bed — composes the
origin/main:charly.yml:2834:check-preempt-local:
origin/main:charly.yml:2836:        from: check-preempt-app
origin/main:charly.yml:2847:                - not_matches: '(^|[[:space:]])check-preempt-local([[:space:]]|$)'

$ gh pr diff 625 --repo opencharly/charly | grep -nE '^-# check-preempt-local|^-check-preempt-(app|local):|^-        from: check-preempt-app|^-            - check: command=charly preempt status'
930:-# check-preempt-local — R10 witness for the externalized `charly preempt` COMMAND
947:-check-preempt-app:
955:-check-preempt-local:
957:-        from: check-preempt-app
964:-            - check: command=charly preempt status exits 0 and names no lease owned by THIS bed …

$ git -C /tmp/opencode/rem-charly-625 grep -c "check-preempt-local" HEAD -- charly.yml
(exit 1 — no match: PR #625's head tree has no check-preempt-local)
```

## Attribution tier

**fully tested and validated** — the relocated witness's full R10 sequence executed live against a fresh clone of this tree (`charly 2026.249.2125`), the changed step (with the new discriminating assertion) passed on the fresh rebuild at zero warnings/zero errors, and the assertion's discriminating power was proven by a negative control that FAILS.

## Harness rulebook compliance

- **R0 skills:** dispatcher rows matched before the first tool call — loaded `/charly-check:check` (bed model, R10), `/charly-internals:git-workflow` (PR-only landing, body-before-push, update-branch), `/charly-image:layer` (candy/plan-step authoring), `/charly-internals:root-cause-analyzer` (R1).
- **R1 RCA + zero warnings:** the block's findings were root-caused and remediated in this tree (discriminating step + narrowed description + schema reconciliation + corrected test citation + conflict resolution); `charly box validate` reports 0 warnings / 0 errors, the bed's `summary.yml` reports `errors: 0, warnings: 0`, and the only emitted warning is the allowlisted `dev-worktree-binary-notice`.
- **R2 no out-of-scope:** the source-side removal lands in the named companion `opencharly/charly#625`; nothing is parked as "follow-up".
- **R3 no duplication:** the witness has ONE canonical home — the plugin's owning repo; companion #625 deletes the charly-side duplicate.
- **R4 no workaround:** no sleep/retry/shim/magic-number; the witness runs the real `charly preempt status` command on the host.
- **R5 hard cutover:** the relocation IS the cutover; the charly-side entity is removed in `opencharly/charly#625` in the same move.
- **R6 git safety:** feature branch `feat/relocate-check-bed`; no force-push, no history rewrite; the review round adds an append-only commit + a merge of `main`.
- **R7 runtime gate:** the end-to-end `charly check run check-preempt-local` gate ran (not just `go test`), output pasted above.
- **R8 artifact invariants:** `N/A — no Containerfile-generation or ai.opencharly.* label change.`
- **R9 binary == source:** the bed ran on `charly 2026.249.2125` against the fresh clone; no new runtime OS dependency.
- **R10 disposable + coverage:** proven on a `disposable: true` `host: local` target; the witness IS the coverage that fails without candy/plugin-preempt's command renderer (negative control above).
- **RDD:** the dispatch premise was verified live — the run executed this checkout's `candy/plugin-preempt` through `${CHARLY_BIN} preempt status`.
- **ADE:** the template composes the LOCAL `plugin-preempt` candy; `charly box validate` reports 0 errors (4 candies, 1 box, 8 deploys).
- **SDD:** `N/A — no .cue edit.`
- **Hard cutover:** one atomic config change; companion removal is the matching leg; no Phase-2/TODO in scope.
- **Kernel/plugin boundary law:** the witness MOVES OUTWARD out of core (`opencharly/charly`) into the plugin's owning repo; no core/sdk movement.
- **Disposable-only autonomy:** the teardown/rebuild was on a `disposable: true` bed.
- **Clean architecture + Go gates:** `N/A for Go — no Go changed.`
- **CHANGELOG:** `N/A — this repo's tag-on-merge writes CHANGELOG/<CalVer>.md from this PR body at merge (org convention).`

---
*Assisted-by: opencode ollama-cloud/deepseek-v4.1-flash (fully tested and validated)*