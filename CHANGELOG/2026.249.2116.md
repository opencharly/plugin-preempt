# 2026.249.2116 — feat(ir): re-pin spec v0.2026249.2106 (group:-kind removal) + sdk v0.2026249.2114

## Summary

Re-pins candy/plugin-preempt to **spec v0.2026249.2106** (spec #105 — the `group:` kind removal) and **sdk v0.2026249.2114** (the tag that dropped sdk's own dead group-root self-skip guard in `deploykit/bed_session.go`, making the spec-2106 dependency graph compilable).

The migration is compile-level only in this repo: there are **zero `.IsGroup()` call sites** here. The sole consumer is the `ArbiterInvokeInput.IsGroup` **wire field** (`in.IsGroup` at the acquire-shared implied-GPU union, arbiter.go:113), which spec #105 keeps **deliberately** — the `arbiter.cue` `is_group` wire field is a documented residual whose semantics survive the cutover: it is now derived POSITIONALLY core-side (a targetless claimant root carrying deploy-level member siblings — the post-migrate spelling of the former group shape). Its R5 removal rides a later wave PR only if/when the wire field itself dies.

Comment remnants in `gpu_imply.go` / `gpu_imply_test.go` re-spelled to the post-migrate world (no behavior change; the GPU-imply semantics are untouched — a member-bearing targetless root never auto-consumes the GPU).

## How tested

```
$ go build ./...                 # exit=0
$ go vet ./...                   # vet exit=0
$ go test -count=1 ./...
ok      github.com/opencharly/plugin-preempt/candy/plugin-preempt        0.138s
?       github.com/opencharly/plugin-preempt/candy/plugin-preempt/cmd/serve     [no test files]
?       github.com/opencharly/plugin-preempt/candy/plugin-preempt/params        [no test files]
```

Executed B14(d) sweeps on the COMMITTED tree (755c4a1):

```
$ git grep -n 'IsGroup()' -- '*.go' '*.cue' '*.yml'        # exit 1 — zero .IsGroup() call sites
$ git grep -n -iE 'kind:[[:space:]]*group|"group"[[:space:]]*:|ResourceKind.*group|#GroupInput|IsGroup\(\)' -- .
  # exit 1 — zero group-kind remnants
$ git grep -n 'in.IsGroup\|IsGroup bool\|is_group' -- '*.go'
  # arbiter.go:113 (in.IsGroup wire field, kept deliberately) + gpu_imply.go:30 (doc comment) only
```

gofmt -l . → clean. Full dependency graph compiles only with sdk ≥ v0.2026249.2114 (opencharly/sdk #224); pins reflect that.

## Rulebook compliance

- **R0** — skills loaded per dispatcher (git-workflow, strict-policy, root-cause-analyzer); no skill covers this repo's plain Go consumer migration beyond those.
- **R1** — RCA for the initial build failure: the break was INSIDE sdk (deploykit/bed_session.go:71 called the deleted `Deploy.IsGroup()`; every released sdk tag ≤ 2036 carried it). Escalated per the operator ruling; fixed upstream first (sdk #224, tag v0.2026249.2114), then re-pinned here. No remediation preceded the RCA.
- **R2** — no repro steps skipped; all gates run with `-count=1` on the committed tree.
- **R3** — no duplication: the positional is_group derivation lives core-side; this plugin consumes the wire field only.
- **R4** — no workarounds: no local replaces, no pin hand-hacks; the upstream fix was awaited, not routed around.
- **R5** — no legacy path remains: zero group-kind call sites (sweeps above); the kept `is_group` wire field is spec #105's own documented residual, not a local legacy.
- **R6** — feat/ branch, PR-only landing, no force-push, no hook bypass, no main push.
- **R7/R10** — build/vet/test executed on the final committed tree (755c4a1), output pasted above; `git status` clean post-commit.
- **R8/R9** — N/A (no docs/marketplace corpus, no pins outside go.mod).
- **R10 beds** — N/A: no charly.yml plan/check surface changed in this repo (pins-only + comments); the wave's bed proof rides charly's own consumer PR.

## Change classification

- **Change class**: dependency re-pin (contract consumer) + doc-comment remnant cleanup. No runtime behavior change in this repo.
- **Verification gate**: go build/vet/test -count=1 (this repo has no task verify / bed surface).
- **Attribution tier**: fully tested and validated (all gates GREEN on the final committed tree; changed paths — the go.mod graph and the touched comment sites — exercised by the compile + test run).

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*